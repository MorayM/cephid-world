---
import { Image } from 'astro:assets';
import Layout from '../layouts/Brochure.astro';
import shows from '../data/live.json';

import poster from '../images/live-2025-rondo.png';
---

<Layout title='Live' description='Details of upcoming live shows' image="/cover-512.png">
  <h2>Live</h2>
  <section class="show-list">
    {shows.map((show) => (
      <div class="show-item">
        <h4 class="show-location">{show.location}</h4>
        <div>
          <span class="show-date">{new Date(show.date).toDateString()} {show.time}</span>
          <span class="ticket-link"><a href={show.url}>Tickets</a></span>
        </div>
      </div>
    ))}
  </section>
  <Image
    src={poster}
    alt="A poster for the Cephid Live Show at the Rondo Theatre on 8th February 2025"
    width={800}
  />
</Layout>

<style>
  .show-list {
    display: flex;
    flex-direction: column;
  }
  .show-item {
    padding: 16px;
    border: 1px solid #ccc;
    margin-bottom: 8px;
    border-radius: 8px;
  }

  .show-item h4 {
    margin: 0;
    line-height: 1em;
  }
  .show-item div {
    display: flex;
    justify-content: space-between;
    line-height: 1em;
    margin-top: 0.5em;
  }
</style>
